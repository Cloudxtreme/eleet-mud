let React = require('react');

const Game = React.createClass({
  render() {
    return (
      <div className='centered' data-role='game-window'>

      </div>
    );
  }
});

module.exports = Game;
